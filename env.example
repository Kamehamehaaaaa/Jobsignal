# env variables

# ── Kafka ─────────────────────────────────────────────────────────────────────
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# ── Gmail ────────────────────────────────────────────────────────────────────
# Path to the credentials.json downloaded from Google Cloud Console
GMAIL_CREDENTIALS_PATH=secrets/gmail_credentials.json
# Where to cache the OAuth2 access + refresh token
GMAIL_TOKEN_PATH=secrets/gmail_token.json
# The Gmail address to poll
GMAIL_USER_EMAIL=rohitukumarb222@gmail.com
# How often to check for new mail (seconds)
GMAIL_POLL_INTERVAL=60

# ── Outlook ───────────────────────────────────────────────────────────────────
# From your Azure App Registration
OUTLOOK_CLIENT_ID=your-azure-app-client-id
OUTLOOK_TENANT_ID=common
# Where to cache the OAuth2 token
OUTLOOK_TOKEN_PATH=secrets/outlook_token.json
OUTLOOK_POLL_INTERVAL=60

# ── Logging ───────────────────────────────────────────────────────────────────
LOG_LEVEL=INFO

# ── NLP backend ───────────────────────────────────────────────────────────────
# Default — spaCy rules, no extra deps
# (leave USE_DISTILBERT unset)

# Zero-shot DistilBERT

# USE_DISTILBERT=zero_shot

# Fine-tuned — run --train first, highest accuracy
#USE_DISTILBERT=finetuned
#DISTILBERT_MODEL_PATH=models/distilbert_rejection_classifier